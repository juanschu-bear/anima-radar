"use client";
import { FormEvent, useState } from "react";
import { createClient } from "@/lib/supabase/client";
import { SignalMark } from "@/components/AppShell";
export default function LoginPage(){const[sent,setSent]=useState(false);const[error,setError]=useState<string|null>(null);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setError(null);const email=String(new FormData(event.currentTarget).get("email")??"").trim();try{const supabase=createClient();const redirectOrigin=process.env.NEXT_PUBLIC_SITE_URL||window.location.origin;const{error:authError}=await supabase.auth.signInWithOtp({email,options:{emailRedirectTo:`${redirectOrigin}/auth/callback`}});if(authError)throw authError;setSent(true)}catch(cause){setError(cause instanceof Error?cause.message:"We couldn't send the link")}}return <main className="login-page"><section className="login-visual"><div className="brand"><SignalMark/><span>Anima<span style={{color:"var(--champagne)"}}>Radar</span></span></div><div className="login-quote"><h1>See the signal before it becomes obvious.</h1><p>A quieter, sharper way to find the businesses already leaning toward what you sell.</p></div><small style={{color:"#708198",fontSize:10}}>PRIVATE MARKET INTELLIGENCE · 2026</small></section><section className="login-form-side"><div className="login-card"><SignalMark small/><h1>Welcome back.</h1><p>Sign in to your workspace with a secure magic link. No password to remember.</p>{sent?<div className="notice" aria-live="polite">Check your inbox. Your secure link is on its way.</div>:<form onSubmit={submit}><label>Email address<input required name="email" autoComplete="email" spellCheck={false} type="email" placeholder="you@company.com" /></label><button className="button button-primary">Send magic link <span className="button-arrow" aria-hidden="true" /></button>{error&&<p className="error" aria-live="polite">{error}</p>}</form>}<p className="login-fine">By continuing, you agree to use AnimaRadar responsibly and respect every recipient’s opt-out.</p></div></section></main>}
