"use client";

import { useEffect, useState } from "react";
import Link from "next/link";

const demoProspects = [{ name: "Granville Island Florist", type: "Florist · Vancouver", score: 87, reasons: ["Offers same-day delivery across Vancouver", "Wedding work is visible in recent reviews", "Published phone and retail location"], facts: ["website", "4.8 ★", "retail"], message: "Hi Taylor — your recent wedding work in Vancouver stood out, especially the seasonal installations mentioned in your reviews. Andes Bloom helps local florists source distinctive Ecuadorian stems with reliable weekly supply. Would a 15-minute conversation next week be useful?\n\nTaylor" }, { name: "Bloom & Co.", type: "Flower shop · Vancouver", score: 78, reasons: ["Local storefront and event delivery", "Seasonal arrangements are a focus", "Clear published contact route"], facts: ["website", "4.6 ★", "events"], message: "Hi Taylor — I noticed your seasonal arrangements and event delivery service. Andes Bloom can help Vancouver shops keep distinctive stems available through the year. Would it be useful to compare notes on your next seasonal order?\n\nTaylor" }];

export default function ProspectsPage() { const [index, setIndex] = useState(0); const [status, setStatus] = useState("new"); const prospect = demoProspects[index]; useEffect(() => { const onKey = (event: KeyboardEvent) => { if (event.target instanceof HTMLTextAreaElement) return; if (event.key.toLowerCase() === "a") setStatus("approved"); if (event.key.toLowerCase() === "d") setStatus("discarded"); if (event.key === "ArrowRight") { setIndex((value) => (value + 1) % demoProspects.length); setStatus("new"); } }; window.addEventListener("keydown", onKey); return () => window.removeEventListener("keydown", onKey); }, []); return <main className="min-h-screen px-5 py-8 md:px-12 md:py-12"><div className="mx-auto max-w-6xl"><div className="flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-[.3em] text-[var(--accent)]">PROSPECTOS / REVISIÓN</p><p className="mt-3 text-sm text-[var(--muted)]">{index + 1} de 212 revisados · teclado activo</p></div><Link href="/enviar" className="text-sm font-bold underline">Ir a enviar →</Link></div><section className="mt-10 grid gap-8 lg:grid-cols-[.8fr_1.2fr]"><div className="rounded-3xl bg-[var(--ink)] p-7 text-[var(--paper)]"><p className="text-xs uppercase tracking-[.2em] text-white/50">Puntaje</p><div className="mt-5 text-8xl">{prospect.score}</div><p className="mt-2 text-white/60">probabilidad de encaje</p><div className="mt-10 space-y-5">{prospect.reasons.map((reason) => <p key={reason} className="border-t border-white/15 pt-4 text-sm leading-6">{reason}</p>)}</div><div className="mt-8 flex flex-wrap gap-2">{prospect.facts.map((fact) => <span key={fact} className="rounded-full bg-white/10 px-3 py-1 text-xs">{fact}</span>)}</div></div><div className="rounded-3xl border border-black/10 bg-white/50 p-7"><p className="text-xs font-bold uppercase tracking-[.2em] text-[var(--accent)]">{prospect.type}</p><h1 className="mt-3 text-5xl">{prospect.name}</h1><p className="mt-3 text-sm text-[var(--muted)]">Mejor canal: business_published_contact</p><textarea defaultValue={prospect.message} className="mt-8 min-h-[260px] w-full rounded-2xl border border-black/10 bg-transparent p-5 text-sm leading-7 outline-none focus:border-[var(--accent)]" /><div className="mt-6 flex flex-wrap gap-3"><button onClick={() => setStatus("approved")} className="rounded-full bg-[var(--ink)] px-5 py-3 text-sm font-bold text-[var(--paper)]">A · Aprobar</button><button onClick={() => setStatus("discarded")} className="rounded-full border border-black/15 px-5 py-3 text-sm font-bold">D · Descartar</button><span className="rounded-full px-4 py-3 text-sm text-[var(--accent)]">{status}</span></div></div></section><div className="mt-6 flex justify-between text-xs text-[var(--muted)]"><span>E editar · D descartar · → siguiente</span><button className="font-bold text-[var(--ink)]">Aprobar todos ≥ 70</button></div></div></main>; }
