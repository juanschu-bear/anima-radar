"use client";

import Link from "next/link";
import { useEffect, useState } from "react";
import AppShell, { ButtonArrow, SectionHeading } from "@/components/AppShell";
import { useLanguage } from "@/components/LanguageProvider";

type Prospect = { id: string; name: string; category: string | null; address: string | null; city: string | null; country: string | null; website: string | null; phone: string | null; email: string | null; instagram: string | null; rating: number | null; review_count: number | null; score: number | null; score_reasons: unknown; best_channel: string | null; status: string; created_at: string };

function reasons(value: unknown) { if (!Array.isArray(value)) return []; return value.map((item) => typeof item === "string" ? item : item && typeof item === "object" && "reason" in item ? String(item.reason) : "").filter(Boolean); }

export default function ProspectsPage() {
  const { text } = useLanguage();
  const [prospects, setProspects] = useState<Prospect[]>([]); const [selectedId, setSelectedId] = useState(""); const [loading, setLoading] = useState(true); const [error, setError] = useState<string | null>(null);
  useEffect(() => { let live = true; fetch("/api/prospects", { cache: "no-store" }).then(async (response) => { const body = await response.json(); if (!response.ok) throw new Error(body.detail); if (live) { setProspects(body.prospects ?? []); setSelectedId(body.prospects?.[0]?.id ?? ""); } }).catch((cause) => { if (live) setError(cause instanceof Error ? cause.message : text("Could not load prospects", "No se pudieron cargar los prospectos")); }).finally(() => { if (live) setLoading(false); }); return () => { live = false; }; }, [text]);
  const selected = prospects.find((item) => item.id === selectedId) ?? prospects[0];
  async function update(status: "approved" | "discarded") { if (!selected) return; setError(null); const response = await fetch("/api/prospects", { method: "PATCH", headers: { "content-type": "application/json" }, body: JSON.stringify({ id: selected.id, status }) }); const body = await response.json(); if (!response.ok) { setError(body.detail); return; } setProspects((current) => current.map((item) => item.id === selected.id ? { ...item, status } : item)); }
  return <AppShell><div className="page-toolbar"><SectionHeading eyebrow={text("Prospects / review queue", "Prospectos / cola de revisión")} title={text("Review the signal, then decide.", "Revisa la señal y decide.")} detail={text("Only real prospects discovered for the active company appear here.", "Aquí solo aparecen prospectos reales descubiertos para la empresa activa.")} /><ButtonArrow href="/enviar">{text("Open outreach", "Abrir contacto")}</ButtonArrow></div>{error && <p className="error" aria-live="polite">{error}</p>}{loading ? <section className="panel empty-state">{text("Loading prospects…", "Cargando prospectos…")}</section> : !selected ? <section className="panel empty-workspace"><span className="empty-orbit"/><p className="eyebrow">{text("No sample data", "Sin datos de muestra")}</p><h2>{text("No prospects for this company yet.", "Aún no hay prospectos para esta empresa.")}</h2><p>{text("Create a radar scan. Discovered businesses will enter this review queue with their evidence and score.", "Crea un escaneo de radar. Las empresas descubiertas entrarán en esta cola con sus evidencias y puntuación.")}</p><Link href="/radar" className="button button-primary">{text("Create a scan", "Crear un escaneo")}<span className="button-arrow"/></Link></section> : <><div className="review-layout"><aside className="panel score-panel"><span className="live-label">{selected.status}</span><div className="score-big">{selected.score ?? "—"}</div><div className="score-caption">{text("likelihood of fit", "probabilidad de encaje")}</div><div className="reason-list">{reasons(selected.score_reasons).length ? reasons(selected.score_reasons).map((reason) => <div className="reason" key={reason}>{reason}</div>) : <div className="reason">{text("No scoring reasons recorded yet", "Aún no hay motivos de puntuación registrados")}</div>}</div></aside><section className="panel review-main"><span className="eyebrow">{[selected.category, selected.city, selected.country].filter(Boolean).join(" · ")}</span><h2>{selected.name}</h2><p className="review-meta">{text("Best available channel", "Mejor canal disponible")}: {selected.best_channel ?? text("not identified", "no identificado")}</p><div className="prospect-facts"><div><span>{text("Address", "Dirección")}</span><strong>{selected.address ?? "—"}</strong></div><div><span>{text("Website", "Sitio web")}</span><strong>{selected.website ?? "—"}</strong></div><div><span>{text("Rating", "Valoración")}</span><strong>{selected.rating ? `${selected.rating} · ${selected.review_count ?? 0} ${text("reviews", "reseñas")}` : "—"}</strong></div></div><div className="review-actions"><button onClick={() => update("approved")} className="button button-primary">{text("Approve", "Aprobar")}<span className="button-arrow">↗</span></button><button onClick={() => update("discarded")} className="button button-ghost">{text("Discard", "Descartar")}</button><span className="status-text">{selected.status}</span></div></section></div><div className="table-panel panel"><div className="table-row table-head"><span>{text("Prospect", "Prospecto")}</span><span>{text("Status", "Estado")}</span><span>{text("Score", "Puntuación")}</span><span>{text("Location", "Ubicación")}</span><span>{text("Action", "Acción")}</span></div>{prospects.map((item) => <button key={item.id} onClick={() => setSelectedId(item.id)} className="table-row data-row"><span className="prospect-name"><span className="company-mark">{item.name.slice(0, 1)}</span>{item.name}</span><span className="status-chip">{item.status}</span><span className="score">{item.score ?? "—"}</span><span className="muted">{item.city ?? "—"}</span><span className="muted">{text("Open", "Abrir")} ↗</span></button>)}</div></>}</AppShell>;
}
